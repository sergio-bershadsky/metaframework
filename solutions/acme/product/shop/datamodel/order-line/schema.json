{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost:3000/schemas/acme/product/shop/datamodel/order-line",
  "title": "Order line",
  "description": "One sellable item at the price and quantity the customer agreed to.",
  "type": "object",
  "properties": {
    "sku": {
      "type": "string",
      "maxLength": 64,
      "description": "Catalog identifier of the sellable item."
    },
    "title": {
      "type": "string",
      "maxLength": 200,
      "description": "Display name captured at the moment of sale."
    },
    "quantity": {
      "$ref": "#/$defs/positive-int",
      "description": "Number of units on this line."
    },
    "unit-price": {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/money",
      "description": "Price of one unit, captured when the line was created."
    },
    "line-total": {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/money",
      "description": "Quantity times unit price under the rounding policy in force. Gross: the discount below is not subtracted from it."
    },
    "line-discount": {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/money",
      "description": "This line's allocated share of the order's total discount, fixed when the cart became an order. Absent on an undiscounted line."
    }
  },
  "required": [
    "sku",
    "quantity",
    "unit-price"
  ],
  "$defs": {
    "positive-int": {
      "type": "integer",
      "minimum": 1
    }
  }
}
