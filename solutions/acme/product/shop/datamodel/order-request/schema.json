{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.metaframework.dev/acme/product/shop/datamodel/order-request",
  "x-srn": "srn://acme/product/shop/datamodel/order-request",
  "title": "Order request",
  "description": "Wire payload of submit-order. Never persisted in this shape.",
  "type": "object",
  "properties": {
    "cart-id": {
      "type": "string",
      "format": "uuid",
      "description": "The cart this request converts into an order."
    },
    "idempotency-key": {
      "type": "string",
      "minLength": 8,
      "maxLength": 128,
      "description": "Client-generated key; a replay with the same key must not charge twice."
    },
    "lines": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "https://schemas.metaframework.dev/acme/product/shop/datamodel/order-line"
      },
      "description": "Lines as the customer confirmed them, echoed back for verification."
    },
    "payment": {
      "$ref": "https://schemas.metaframework.dev/acme/product/shop/datamodel/payment-method",
      "description": "Tagged payment instrument."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Contact address; the only identity a guest checkout carries."
    }
  },
  "required": [
    "cart-id",
    "idempotency-key",
    "lines",
    "payment"
  ]
}
