{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost:3000/schemas/acme/product/shop/datamodel/order-placed",
  "title": "Order placed",
  "description": "Public fact published when an order is accepted by checkout.",
  "type": "object",
  "allOf": [
    {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/base-record"
    }
  ],
  "properties": {
    "order-id": {
      "type": "string",
      "format": "uuid",
      "description": "Identity of the order, stable across every later fact about it."
    },
    "customer-id": {
      "type": "string",
      "description": "Pseudonymous customer reference, replaced on erasure."
    },
    "total": {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/money",
      "description": "Gross amount of the order."
    },
    "lines": {
      "type": "array",
      "items": {
        "$ref": "http://localhost:3000/schemas/acme/product/shop/datamodel/order-line"
      },
      "description": "The lines as placed."
    },
    "placed-at": {
      "type": "string",
      "format": "date-time",
      "description": "When the customer's submission was accepted."
    }
  },
  "required": [
    "order-id",
    "total"
  ]
}
