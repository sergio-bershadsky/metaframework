{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost:3000/schemas/acme/product/shop/component/checkout/datamodel/cart",
  "title": "Cart",
  "description": "Mutable basket owned by checkout. Never leaves the component.",
  "type": "object",
  "allOf": [
    {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/base-record"
    }
  ],
  "properties": {
    "customer-id": {
      "type": "string",
      "description": "Account or guest-session reference; pseudonymous after erasure."
    },
    "currency": {
      "type": "string",
      "enum": [
        "EUR",
        "GBP",
        "USD"
      ],
      "description": "Fixed at creation. One currency per cart, per ADR 0001."
    },
    "lines": {
      "type": "array",
      "items": {
        "$ref": "http://localhost:3000/schemas/acme/product/shop/datamodel/order-line"
      },
      "description": "Lines as the customer has built them so far."
    },
    "subtotal": {
      "$ref": "http://localhost:3000/schemas/acme/datamodel/money",
      "description": "Sum of the line totals, before tax and shipping."
    },
    "expires-at": {
      "type": "string",
      "format": "date-time",
      "description": "After this instant checkout refuses to convert the cart."
    }
  },
  "required": [
    "customer-id",
    "currency",
    "lines"
  ]
}
