{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "http://localhost:3000/schemas/acme/datamodel/money",
  "title": "Money",
  "description": "An exact monetary amount and the currency it is denominated in.",
  "type": "object",
  "properties": {
    "amount": {
      "type": "string",
      "pattern": "^-?[0-9]+\\.[0-9]{2}$",
      "description": "Decimal string with exactly two fractional digits. Never a float."
    },
    "currency": {
      "type": "string",
      "enum": [
        "EUR",
        "GBP",
        "USD"
      ],
      "description": "ISO 4217 alphabetic code. The set is closed by ADR 0001."
    }
  },
  "required": [
    "amount",
    "currency"
  ]
}
