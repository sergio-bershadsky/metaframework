{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "x-srn": "srn://acme/shop/datamodel/sepa-payment",
  "title": "SEPA payment",
  "description": "SEPA direct debit instrument, authorized by a mandate held at the acquirer.",
  "type": "object",
  "properties": {
    "method": {
      "const": "sepa",
      "description": "Union tag."
    },
    "iban": {
      "type": "string",
      "pattern": "^[A-Z]{2}[0-9]{2}[A-Z0-9]{10,30}$",
      "description": "Account the debit is drawn on."
    },
    "mandate-id": {
      "type": "string",
      "maxLength": 64,
      "description": "Reference of the mandate authorizing acme to debit this account."
    },
    "creditor-id": {
      "type": "string",
      "maxLength": 64,
      "description": "Acme's SEPA creditor identifier used for this debit."
    }
  },
  "required": [
    "method",
    "iban",
    "mandate-id"
  ]
}
